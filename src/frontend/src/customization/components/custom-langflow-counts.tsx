import { LangflowCounts } from "@/components/core/appHeaderComponent/components/flow-counts";

export function CustomLangflowCounts() {
  return <LangflowCounts />;
}

export default CustomLangflowCounts;
