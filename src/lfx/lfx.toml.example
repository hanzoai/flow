# Hanzo Flow Service Configuration
#
# This file tells LFX to use Hanzo Flow's full-featured service implementations
# instead of the minimal/noop services built into LFX.
#
# Usage:
#   1. Copy this file to your FLOW_CONFIG_DIR as lfx.toml
#   2. Or add to pyproject.toml under [tool.lfx.services]
#
# The services will be automatically discovered and loaded when you run:
#   - flow run
#   - lfx serve (when run from flow project)

[services]
# Core Services - Full-featured Hanzo Flow implementations
database_service = "flow.services.database.service:DatabaseService"
storage_service = "flow.services.storage.local:LocalStorageService"
cache_service = "flow.services.cache.service:ThreadingInMemoryCache"

# Authentication & Session Management
auth_service = "flow.services.auth.service:AuthService"
session_service = "flow.services.session.service:SessionService"

# Chat & Communication
chat_service = "flow.services.chat.service:ChatService"

# State & Data Management
state_service = "flow.services.state.service:StateService"
variable_service = "flow.services.variable.service:VariableService"

# Background Processing
task_service = "flow.services.task.service:TaskService"
job_queue_service = "flow.services.job_queue.service:JobQueueService"

# Marketplace & Components
store_service = "flow.services.store.service:StoreService"
shared_component_cache_service = "flow.services.shared_component_cache.service:SharedComponentCacheService"

# Observability
telemetry_service = "flow.services.telemetry.service:TelemetryService"
tracing_service = "flow.services.tracing.service:TracingService"

# Note: settings_service is NOT configurable - it always uses the built-in implementation
# Note: mcp_composer_service uses the lfx implementation by default
